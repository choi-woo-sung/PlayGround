<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.playground">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayGround"
        tools:targetApi="31">
        <activity
            android:name=".scroll.ScrollActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".download.DownloadActivity"
            android:exported="true"
            android:label="@string/title_activity_download"
            android:theme="@style/Theme.PlayGround">
        </activity>

        <activity
            android:name=".sim.SimOperatorActivity"
            android:exported="false"></activity>
        <activity
            android:name=".overlap.OverlapActivity"
            android:exported="false" />
        <activity
            android:name=".memorydump.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.PlayGround.NoActionBar" />
    </application>

</manifest>